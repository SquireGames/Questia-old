
cmake_minimum_required (VERSION 2.8.10)

project(${DP_ARG_PROJECT}-download NONE)

include(ExternalProject)
ExternalProject_Add(
    ${DP_ARG_PROJECT}-download
    GIT_REPOSITORY      "${DP_ARG_REPOSITORY}"
    GIT_TAG             "${DP_ARG_COMMIT}"
    SOURCE_DIR          "${DP_SOURCE_DIR}"
    BINARY_DIR          "${DP_BINARY_DIR}"
    CONFIGURE_COMMAND   ""
    BUILD_COMMAND       ""
    INSTALL_COMMAND     ""
    TEST_COMMAND        ""
)
